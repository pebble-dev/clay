'use strict';

export = {
  name: 'submit',
  template: require('../../templates/components/submit.tpl'),
  style: require('../../../tmp/submit.css'),
  manipulator: 'button',
  defaults: {
    attributes: {}
  }
};
